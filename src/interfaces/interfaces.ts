export interface IMsgData {
    NOME_CONTATO?: string
    NUMERO_CONTATO?: string
    POLITICAS_PRIVACIDADE?: string
    MENSAGEM?: string
}

export interface ImsgList {
    tel: string,
    os: string
}